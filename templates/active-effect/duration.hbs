<section class="tab {{#if tabs.duration.active}}active{{/if}}" data-group="main" data-tab="duration">
  {{#each duration}}
  <fieldset class="card">
    <legend>{{localize label}}</legend>
    {{#each fields}}
    {{formGroup field value=value enriched=enriched disabled=disabled height=height options=choices}}
    {{/each}}
  </fieldset>
  {{/each}}
</section>
